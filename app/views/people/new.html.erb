<h1>Nouveau Patient</h1>

<%= form_for(@person) do |f| %>
   <%= f.error_messages %>
   
   <%= render "form_#{@person.current_step}", :f => f  %>
   
   <div class="actions">
      <%= f.submit "Créer" %>
   </div>
<% end %>
